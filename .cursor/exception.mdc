---
description: 
globs: 
alwaysApply: true
---
- 如无必要，不要 try-except 捕获异常，交由全局异常处理器统一处理
- 如无必要，不要自定义异常类
- 如果确实需要 try-except 捕获异常，只在必要的代码块使用 try-except, 避免大范围捕获异常
- 如果确实需要创建自定义异常类，异常类需要表达清晰，对开发者或用户友好
- 函数开头优先处理错误和边界条件
- 记录异常日志时需要包含类型、堆栈和上下文信息
- 使用中间件统一处理未捕获异常、日志记录和监控
